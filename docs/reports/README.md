# FER3ON Reports

This directory is the canonical home for new project reports.

Historical reports in the repository root are preserved for compatibility and
are not runtime inputs. Runtime analytics live under `data/analytics`; generated
runtime data is not part of the source-of-truth code review.

Use `STATUS.md` for the current implementation and validation state.
