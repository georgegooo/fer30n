# تنبيه صريح: حدود التحقق من جدوى الاستراتيجية (Backtest Validity)

## الوضع الحالي (قبل هذا التعديل)

`run_backtest.py` كان يعمل بأحد مسارين فقط:

1. **MT5 متصل فعليًا** → بيانات حقيقية (`copy_rates_from_pos`) — هذا المسار سليم إن توفر.
2. **لا MT5 / فشل الاتصال** → يتحول تلقائيًا وبصمت إلى `_build_synthetic_rates()`،
   وهي بيانات **عشوائية بالكامل** (random walk بـ `numpy.random.default_rng`)
   لا علاقة لها بسلوك السوق الفعلي (لا اتجاهات حقيقية، لا جلسات، لا أخبار،
   لا سلوك سيولة). أي تقرير أداء (Win Rate, Profit Factor, Sharpe...)
   ناتج عن هذا المسار **لا يثبت شيئًا عن جدوى القواعد أو الاستراتيجيات**
   — هذه بالضبط نقطة الضعف #2 التي رُصدت.

المشكلة الحقيقية لم تكن وجود fallback اصطناعي بحد ذاته (مفيد كـ smoke test
تقني)، بل أنه كان **صامتًا وغير مُعلَّم** في المخرجات، بما يسمح بالخلط بينه
وبين تحقق فعلي.

## ما تم إصلاحه في هذا التعديل

1. تحذير صريح مطبوع عند استخدام بيانات اصطناعية (`⚠️ SYNTHETIC`).
2. حقل `data_source` صريح في كل تشغيل (`real_mt5` / `real_csv` / `synthetic`)
   مطبوع في رأس التقرير مباشرة، بلا التباس.
3. مسار جديد `--csv <path>` لتحميل بيانات تاريخية حقيقية (مُصدَّرة من
   MT5 أو أي منصة) بلا الحاجة لاتصال حي بـ MT5 وقت التشغيل.

## ما لم يُصلَح بعد (ويجب أخذه بالاعتبار)

- **لا يوجد حتى الآن أي تقرير باك-تيست فعلي داخل المشروع نفسه** مبني على
  بيانات تاريخية حقيقية لـ XAUUSD (أو أي رمز) يثبت جدوى القواعد الحالية
  (SMC / price action / risk rules) بأرقام Win Rate / Profit Factor /
  Max Drawdown موثوقة. توفير `--csv` هو الأداة، لكن **تشغيله فعليًا على
  بيانات كافية (يفضَّل آلاف الشموع عبر عدة سنوات وأنظمة سوق مختلفة) ثم
  حفظ النتائج، لم يحدث بعد.**
- Walk-forward و Monte Carlo الموجودان في `testing/backtester.py` أدوات
  سليمة منهجيًا، لكنهما عديما القيمة إن غُذِّيا ببيانات اصطناعية —
  التأكد من `data_source` قبل الثقة بأي تقرير أصبح الآن ممكنًا وواضحًا،
  لكنه يتطلب انتباه المستخدم عند كل تشغيل.
- لا توجد آلية تمنع برمجيًا استخدام نتائج `synthetic` كمدخل لمعايرة
  `validated_win_rate` في `data/knowledge/structured/rules.json` —
  هذا الحقل موجود كخطاف مستقبلي فقط ويبقى `null` حتى تُغذَّى القواعد
  يدويًا من تقرير موسوم `real_mt5` أو `real_csv`.

## التوصية العملية

قبل الاعتماد على أي إشارة من البوت في حساب حقيقي:
1. صدّر بيانات تاريخية حقيقية (CSV) لعدة سنوات من XAUUSD.
2. شغّل: `python run_backtest.py --csv path/to/real_data.csv --mode full`
3. تحقق أن رأس التقرير يظهر `Data source: REAL_CSV ✅` وليس `SYNTHETIC`.
4. لا تعامل نتيجة `SYNTHETIC` كدليل جدوى بأي حال — فائدتها الوحيدة
   اختبار أن الكود لا يتعطل تقنيًا (smoke test).
