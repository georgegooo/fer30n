# FER3ON-MASR-FAIE v1 — ملخص التنفيذ

تنفيذ التوصيات الواردة في التقرير المُرفَق، بنفس ترتيب أقسامه (أ/ب/ج).
كل بند يشير إلى الـ commits ذات الصلة في `git log`. **479/479 اختبار
ناجح** (كانت 449 قبل البدء؛ أضفنا 30 اختبارًا جديدًا لتغطية كل ميزة جديدة)
— لا شيء انكسر.

---

## ب) الأعلى تأثيرًا على الربحية

### 🔴 إصلاح enrichment fields المفقودة في ai_memory.csv — **الأهم، ونُفِّذ بالكامل**

الفحص كشف أن المشكلة أعمق مما وصفه التقرير: ليست فقط أن الحقول "فارغة"،
بل أن **كل صفقة تقريبًا كانت تُسجَّل مرتين** — مرة بحالة `OPEN` تبقى
معلَّقة للأبد، ومرة أخرى بصف SYNC منفصل بلا أي بيانات. السبب: مزامنة
الإغلاق (`core/mt5_history_sync.py`) كانت تُطابق الصفقات بـ `deal.ticket`
بينما التسجيل عند الفتح (`core/trade_executor.py`) يُطابق بـ
`result.order` — معرّفان مختلفان في MT5 لا يتطابقان عمليًا أبدًا. تحقّقتُ
من ذلك على بيانات المشروع الفعلية: **728 صف عالق على OPEN، و695 صف SYNC
منفصل، صفر تداخل بينهما.**

المُنفَّذ:
- تصحيح معرّف المطابقة في `mt5_history_sync.py` (استخدام
  `deal.position_id` كما هو مُستخدَم بالفعل لجزء آخر من نفس الدالة).
- إصلاح جذري في `core/ai_memory.py`: قيم افتراضية بها فخ — أي استدعاء
  لاحق لـ `save_trade_memory()` كان يمحو القيم الجيدة السابقة بالقيمة
  الافتراضية (0 / UNKNOWN) حتى لو لم يقصد المستدعي ذلك، لأن بايثون يملأ
  القيم غير المُمرَّرة بالافتراضي دائمًا. أُضيف sentinel يُميّز "لم يُمرَّر"
  عن "مُرَّر فعليًا 0".
- تمرير choch_state/choch_strength/liq_map_score/liq_map_dir/
  mtf_strength/mtf_structural فعليًا من `main.py` (مسار SMC) و
  `core/strategy_runners.py` (SCALP/SWING/MICRO) إلى `save_trade_memory()`
  — كانت هذه القيم تُحسَب بالفعل للـ ML feature extraction لكن لا تصل
  أبدًا لسجل الذاكرة.
- تحقّق يدوي (محاكاة فتح→إغلاق) يؤكد بقاء atr/exec_grade/
  choch_strength/liq_map_score/liq_map_dir/mtf_structural بعد الدمج بدل
  مسحها.

### 🔴 Order Flow حقيقي بدل synthetic — **لم يُنفَّذ (يحتاج بيانات لا أملكها)**

هذا يتطلب اتصالاً حيًا بـ Level 2 / DOM من بروكر فعلي — بيانات سوق حية
غير متاحة لي في هذه البيئة. لا يوجد بديل هندسي بدون هذا الاتصال؛ أي محاولة
لمحاكاته ستكون تخمينًا. البنية الحالية (`_candle_weight()` /
`_volume_expansion()` في `core/synthetic_orderflow.py`) جاهزة لاستقبال
بيانات حقيقية بنفس الواجهة (Analyst API) بمجرد توفر اتصال بروكر يوفرها.

### 🟡 TP Capping — تليمتري للتحقق التدريجي

الميزة كانت مُفعَّلة بالفعل وبلا شرط، لكن بلا أي تسجيل لعدد مرات
تفعيلها. أُضيف `analytics/tp_cap_monitor.py`: يسجّل كل حدث قص فعلي (متى،
كم قُصَّت، لأي استراتيجية) في `data/analytics/tp_cap_events.csv`، حتى
تكون فترة التحقق على بيانات حية (المُوصى بها في التقرير) قابلة للقياس
بأرقام حقيقية بدل قراءة سطور `print()`.

### 🟡 قياس أثر Portfolio Risk Authority

أُضيف `analytics/authority_impact.py`. بما أن "النسخة القديمة" ليست
خوارزمية بديلة (تأكّد ذلك من تعليق فعلي في main.py: evaluate_risk() لم
تكن تُستدعى إطلاقًا قبل الإصلاح — هذا هو سبب حادثة 149 صفقة متزامنة)، فإن
خط الأساس القديم ببساطة "كل صفقة تُنفَّذ باللوت المطلوب، بلا أي بوابة".
الموديول يسجّل كل قرار Authority حقيقي بجانب ما كانت ستفعله هذه القاعدة
القديمة، ويحسب: كم صفقة رُفضت، لأي سبب، وكم % مخاطرة قُصَّت إجمالًا.

### 🟢 Regime-Aware Position Sizing — **نُفِّذ بصيغة مختلفة عن المقترح، عن قصد**

⚠️ هذا البند يستحق انتباهك: التقرير يقترح ضرب اللوت الحي مباشرة بمعامل
regime edge. لكن `analytics/regime_edge_analysis.py` هو موديول "Phase 2"
مُقيَّد صراحة بعقد أمان موثَّق في المشروع نفسه
(`PHASE_2_SAFETY_BOUNDARY.md`): "zero runtime influence"،
`PHASE2_RUNTIME_INFLUENCE = False # NEVER flip to True`، وكل ناتج منه
يحمل `advisory_only=True`. كما أن main.py و core/strategy_runners.py
(حيث يُحسَب اللوت الفعلي) مُدرَجان صراحة في قائمة "Files NEVER to touch"
لهذا الموديول.

تنفيذ اقتراح التقرير حرفيًا كان سيكسر قيدًا أمانيًا متعمدًا وضعه فريق
المشروع نفسه. بدلًا من ذلك: `analytics/regime_sizing_shadow.py` يُعيد
تشغيل (replay) تاريخ الصفقات المُغلقة الفعلي — الذي أصبح الآن يحمل
market_regime صحيحًا بفضل إصلاح enrichment — ويحسب: "لو كان هذا المعامل
مُطبَّقًا، ماذا كانت النتيجة التراكمية؟" بيانات حقيقية، offline بالكامل،
صفر لمس لأي ملف على القائمة المحظورة. الترقية لتأثير حي فعلي قرار واعٍ
منفصل يتطلب تعديل ملف الأمان نفسه أولًا.

### 🟢 Session-Aware Filtering — **نفس المنطق أعلاه بالضبط**

`analytics/session_edge_analysis.py` هو أيضًا موديول Phase 2 بنفس القيد.
`analytics/session_filter_shadow.py` يجيب على سؤال التقرير ("لو مُنعت
الجلسات ضعيفة الأداء، كم صفقة كانت ستُمنع وما ربحها/خسارتها الفعلية؟")
عبر إعادة تشغيل تاريخي حقيقي، بدل فلتر حي في main.py.

### 🟢 Shadow Backtest إلزامي قبل كل merge

`tools/ci_shadow_backtest_gate.py` + `.github/workflows/shadow_backtest_gate.yml`.
يُشغّل الاستراتيجية ويُفشل البناء لو تراجعت Expectancy أكثر من 5% —
**لكن فقط عند توفر بيانات حقيقية عبر `--csv`**. لا توجد بيانات OHLC حقيقية
مُرفقة في هذا المستودع، وبيانات synthetic لا يجوز اعتبارها دليل جدوى (هذا
موثَّق صراحة في `BACKTEST_VALIDITY_NOTICE.md` الموجود أصلًا في مشروعك).
لذلك في وضعه الحالي، البوابة تعمل كـ **smoke test فقط** (هل الكود ينهار؟)
حتى تُضيف بيانات تاريخية حقيقية لآخر ~90 يومًا تحت `data/backtest_data/`.

---

## أ) التنظيف

| البند | ماذا حدث |
|---|---|
| دمج تقارير الحالة | 17 ملف *_status_report.md ونحوه → `docs/history/` مع فهرس. أُبقيت في الجذر فقط الملفات النشطة فعليًا: `PHASE_2_SAFETY_BOUNDARY.md`، `BACKTEST_VALIDITY_NOTICE.md`، و`certification_framework.md`/`certification_status.md` (تأكّد أن الكود يكتبها فعليًا وقت التشغيل — نقلها كان سيُنشئ نسخة قديمة مربكة). `CHANGELOG.md` أصبح الوحيد في الجذر. |
| توحيد run_*.py | `python -m fer3on <command>` (masr/backtest/faie-backtest/faie-shadow/audit/train/ci-gate) — طبقة إضافية فوق السكربتات الأصلية، لم تُحذف أي منها (لا كسر لأي أتمتة موجودة). |
| alias detect_vwap_reclaim_candle | تأكّدت أنه لا يُستخدَم في أي كود إنتاجي، فحُذف. |
| rotation policy لـ data/backups | `tools/backup_restore.py`: `create_dated_backup()` + `rotate_backups()` — نُسخ احتياطية بطابع زمني، تحذف تلقائيًا ما يتجاوز عددًا معينًا أو عمرًا معينًا. |
| (إضافي، لم يُطلب صراحة) | لم يكن هناك `.gitignore` إطلاقًا — 278 ملف `.pyc` كانت متتبَّعة بالخطأ. أُضيف `.gitignore` وأُزيلت. |

## ج) ما لم يُضَف (كما طلب التقرير بالضبط)

لم يُضَف أي Strategy Agent جديد، ولا Multi-Asset، ولم يُفعَّل ML كـ
Authority — الثلاثة تُركت كما هي.

---

## أداة إضافية: Development Readiness Gate

بعد مراجعة تقرير "Phase X — Cognitive Institutional Layer" (يقترح 14 نظامًا
معماريًا جديدًا)، كانت التوصية: قبل أي بنية جديدة، أثبت أن الأساسيات
شغالة على بيانات حقيقية. `tools/development_readiness_gate.py` (متاح أيضًا
عبر `python -m fer3on readiness`) يحوّل هذه التوصية لـ checklist فعلي بـ4
معايير:

1. **Data Quality** — نسبة تغطية enrichment fields على آخر 50 صفقة مُغلقة
   في `ai_memory.csv`.
2. **Real Backtest** — هل `tools/ci_shadow_backtest_gate.py` اتشغّل فعليًا
   على بيانات حقيقية (`--csv`)، وليس synthetic فقط؟
3. **Trading Sample** — عدد كافٍ من الصفقات المُغلقة (≥30) عبر مدة كافية
   (≥14 يوم).
4. **Market Diversity** (استشاري، لا يمنع الجاهزية) — هل العيّنة تغطي أكثر
   من نظام سوق واحد؟

شغّلتها فعليًا على بيانات مشروعك الحالية والنتيجة **NOT READY**: تغطية
enrichment = 0% (لأن كل الـ 720 صفقة المُغلقة الموجودة حاليًا اتسجّلت
**قبل** إصلاح enrichment، فمن الطبيعي تكون فارغة)، ولا يوجد baseline
backtest حقيقي بعد. هذا متوقّع ومنطقي — الأداة هتبدأ تدّي PASS تدريجيًا مع
تراكم صفقات جديدة تحت الكود المُصلَح، ومع تشغيل `ci-gate` على بيانات
حقيقية. الأداة لا تمنع أي شيء تلقائيًا (مش CI gate) — هي أداة قرار بشري
تُشغَّل يدويًا قبل البدء في أي تطوير معماري جديد.

15 اختبار جديد لكل معيار + منطق تجميع القرار النهائي.

---

## إصلاح إضافي: execution_grade لم يكن يصل لمحرك القرار الحي

اكتُشف من فحص لوج تشغيل حقيقي على MT5: `build_decision_context()` مالهاش
`execution_grade` parameter خالص، فكانت قيمته تبقى دايمًا 'UNKNOWN' مهما
كان الـ grade الحقيقي المحسوب (B، B+، إلخ) — ده كان بيعطّل بصمت مسار
"تنفيذ ممتاز" (`_is_high_execution_context`) بالكامل، وكان بيفعّل عقوبة
التقلب العالي دايمًا بغض النظر عن جودة التنفيذ الفعلية. تم إصلاحه + 4
اختبارات جديدة.

---

## إصلاح إضافي 2: WATCHDOG كان بيبلّغ DEGRADED على كل هارت-بيت تقريبًا

من لوج تشغيل حقيقي تاني: `WATCHDOG | DEGRADED | issues=STARTUP_BLOCK`
ظهرت بعد كل هارت-بيت، في جلستين مختلفتين بفاصل أكتر من 24 ساعة. السبب:
`production_ready` كان مربوط بـ"هل اتوافقنا على صفقة الهارت-بيت ده" بدل
صحة النظام الفعلية — يعني أي دورة عادية ترفض صفقة (وده الوضع الطبيعي
الغالب) كانت بتتقرأ كـ"النظام متعطّل". اتصلح بربط `production_ready` بفحص
بدء تشغيل حقيقي (`startup_status == SYSTEM_READY`) بيتحسب مرة واحدة فقط،
منفصل تمامًا عن قرار كل صفقة. 3 اختبارات جديدة.

---

## إصلاح إضافي 3: LOSS_PAUSE_GUARD كان ممكن يتوقف للأبد (باگ خطير)

من تالت لوج حي: نفس الحالة بالظبط (`consecutive_losses=2 choch=NONE bos=NONE`)
عبر يومين كاملين بدون أي تغيير. تتبّعت المصدر: `_fetch_choch_bos()` في
`core/loss_pause_guard.py` كانت بتدوّر على مفاتيح (`structure.choch_h1`،
`structure.choch_h4`، `snapshot.choch_state`) **مش موجودة إطلاقًا** في
الـ snapshot اللي main.py بيبنيه — القيمة الحقيقية المحسوبة فعليًا موجودة
في `snapshot['structure_analysis']['choch']`. الدليل القاطع: نفس اللوج
أظهر `Entry Reason: CHOCH | ...` (يعني choch حقيقي اتكشف تلك اللحظة بالظبط)
بينما الحارس طبع `choch=NONE` في نفس الدورة!

**الخطورة**: بعد صفقتين خسارة متتاليتين، الحارس بيدخل وضع انتظار "CHoCH أو
BOS جديد" — لكن ده كان **مستحيل يتحقق أبدًا** لأن الفحص عمياني بنيويًا.
يعني توقف تداول دائم وصامت، بدون أي error، حتى يتدخل حد يدويًا
(`force_reset()`). اتصلح بقراءة `structure_analysis['choch']`/`['bos']`
أولًا (مع الإبقاء على المفاتيح القديمة كـ fallback للتوافق). 7 اختبارات
جديدة — تأكدت فعليًا إن 4 منها كانت بتفشل على الكود القديم قبل الإصلاح.

---

## أداة إضافية 2: تنظيف ai_memory.csv من بيانات ما قبل الإصلاح

بعد ما استخدم المستخدم `scripts/cleanup_demo.py` الموجودة أصلًا لتصفير
`data/history/trades.csv` (بيانات بوت قديم، منطقي جدًا)، لاحظنا إن فيه ملف
تاني منفصل بنفس المشكلة: `data/memory/ai_memory.csv` كان لسه فيه 1448 صف
قديم (728 عالقة على OPEN + 695 صف SYNC يتيم بلا enrichment) من قبل إصلاح
الـ enrichment. أضفنا `scripts/cleanup_ai_memory.py` (متاحة كمان عبر
`python -m fer3on cleanup-memory`) بنفس فلسفة الأداة الموجودة بالضبط:
نسخة احتياطية أولًا دايمًا، `--full` لبداية نظيفة كاملة، أو الوضع
الافتراضي يشيل OPEN العالقة وSYNC اليتيمة ويحتفظ بالصفقات المُغلقة
الحقيقية فقط. جُرِّبت فعليًا على بيانات المشروع الحقيقية (1448→25 صف في
الوضع الافتراضي) والنتيجة صحيحة ومطابقة لما توقعناه من تحليل سابق. 4
اختبارات جديدة + اختبار CLI.

---

## كيف تراجع كل شيء

- `git log --oneline` يعطيك كل الـ commits بالترتيب، كل واحد برسالة تشرح
  **لماذا** لا فقط **ماذا**.
- `python -m pytest tests/ -q` يجب أن يعطي 514 passed, 1 skipped.
- `python -m fer3on readiness` يديك تقييم فوري لحالة البيانات الحالية.
- إن أردت رؤية بيانات المزامنة المُصلَحة عمليًا: شغّل البوت لفترة قصيرة ثم
  افحص `data/memory/ai_memory.csv` — يجب ألا ترى صفوف OPEN عالقة بلا نهاية
  ولا صفوف SYNC منفصلة فارغة.
