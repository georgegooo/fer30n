# FER3ON — Phase 0: Decision Contracts — 2026-08-29

**الحالة:** إضافي بالكامل. صفر تغيير في السلوك الحي — main.py وكل مسارات التنفيذ لسه بالظبط زي ما كانت.

## المرجع
"المرحلة صفر" من الترتيب المقترح في الوثيقتين (Roadmap و"أفضل فكرة للـSL/TP"): تثبيت عقود بيانات موحّدة قبل أي توحيد فعلي للاستراتيجيات أو مسار التنفيذ.

## اللي اتعمل
ملف جديد `core/decision_contracts.py`، فيه:
- `DecisionState` (enum): `REJECTED / WAIT_RETEST / WAIT_CONFIRMATION / APPROVED / EXECUTED / EXPIRED / CLOSED` — يحل تعريف الخطر رقم 8 (مفيش تمييز بين "اتجاه صحيح" و"تنفيذ جاهز").
- `SignalSnapshot`, `RiskDecision`, `FinalEntryPlan`, `ExitPlan`, `ExecutionResult`, `PositionState`, `ShadowOpportunity`, `DecisionResult` — كل واحد `dataclass` بحقوله المحددة في الوثيقتين حرفيًا.
- كل عقد بيحمل `build_id` + `decision_snapshot_id` تلقائيًا (مطلب صريح مذكور في الوثيقة).
- `ShadowOpportunity.shadow_only` بتترجع `True` دايمًا بالـdefault — علم صريح برمجيًا (خطر #7) يقدر أي كود استهلاك مستقبلي يتحقق منه قبل الكتابة في أي ملف تعلّم حي.
- `.to_dict()` على كل عقد للتوافق مع الكود الحالي اللي بيتوقع قواميس.

## ليه ده كل اللي حصل
هذا الملف **مش مستورد من أي كود إنتاجي لسه** — تأكدت بنفسي (واختبار مخصص `test_decision_contracts_module_has_zero_production_importers_yet` بيفشل بوضوح لو حد وصله بدري من غير قصد). توحيد الاستراتيجيات (SMC مقابل SCALP/SWING/MICRO) تحت العقود دي هو المرحلة الفعلية اللي بتغيّر سلوك حي — دي خطوة أكبر وأخطر بكتير، ومش هتتعمل قبل ما تتفق عليها صراحة كخطوة منفصلة.

## التحقق
- `py_compile` + استيراد فعلي: نضيف.
- 6 اختبارات جديدة، كلهم PASS.
- السويت الكامل: **654 passed / 5 failed** (+6 بالظبط، نفس الـ5 المعروفين من قبل).
- `import main` بعد التعديل: شغال زي ما هو، بدون أي تحذير جديد.

## الخطوة التالية (لو عايز تكمل)
المرحلة 1 الفعلية: تمرير SMC عبر نفس العقود دي بدل قواميسها الحرة، وتوحيدها مع SCALP/SWING/MICRO تحت authority واحدة — دي أول خطوة بتلمس تنفيذ حي فعلي، ومحتاجة اتفاق صريح على النطاق قبل البدء.
