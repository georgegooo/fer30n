# =============================================================================
# FER3ON-AI-V2 — Python Dependencies
# =============================================================================

# Core trading runtime
python-dotenv>=0.19.0
requests>=2.28.0
numpy>=1.21.0,<2.5  # joblib legacy model artifacts are incompatible with NumPy 2.5
pandas>=1.3.0
joblib>=1.0.0
scikit-learn>=1.0.0

# ML (optional but recommended)
xgboost>=1.6.0

# Dashboard / visualization
streamlit>=1.35.0
plotly>=5.20.0
streamlit-autorefresh>=1.0.1

# MT5 package is Windows-only
MetaTrader5>=5.0.0; platform_system == "Windows"
