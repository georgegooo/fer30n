# FER3ON — BRAIN-SCOPE Patch (2026-08-19)

امتداد لباتش FER3ON-FIX-2026-08-19. الباتش الأصلي حصّن **التقييم** و**بوابة الدخول**
بفلترة الـ `BUILD_ID`؛ الباتش ده بيوصّل نفس الحماية لـ**العقل اللي بيحسب القرار الحي**.

## المشكلة

`master_score` في `core/brain_unified.py` بيتكوّن من:
- `dna_score` (وزن 35%) — من `brain/trade_dna.py` بيقرا `data/trade_dna_v2.json` كامل
- `history_score` (وزن 30%) — من `brain/history_learner.py` بيقرا `data/memory/ai_memory.csv` كامل (1731 صف)

يعني **65% من قرار التنفيذ** كان بيتغذى بصفقات كل النسخ مخلوطة، بما فيها صفقات
النسخة الفاشلة اللي شخصّها تقرير 2026-08-19 نفسه. ونفس البيانات الغير مفلترة
كانت بتغذي `self_optimizer.py` و`adaptive_weighting.py` اللي بيعدّلوا الأوزان.

## الإصلاح

### 1. `core/build_scope.py` (جديد)
وحدة فلترة مركزية، نفس نمط `expected_edge_gate.py`:
- `is_current_build_row(row)`: صفقة "حالية" = `build_id == BUILD_ID` أو (لو
  مفيش build_id) `timestamp >= BUILD_DEPLOYED_AT`. صفوف `LEGACY_*` و
  `SYNTHETIC_BOOTSTRAP` والقديمة مستبعدة.
- `filter_current_build_rows(rows)`: فلترة قائمة كاملة.
- لو البيئة مفيهاش `BUILD_ID`/`BUILD_DEPLOYED_AT` أصلًا → لا استبعاد
  (توافق مع البيئات القديمة، لا كسر).
- **لا مسح لأي بيانات** — الصفوف القديمة تفضل على الديسك كأرشيف.

### 2. المكونات المعدّلة (كلها تقرأ من البيلد الحالي فقط الآن)

| الملف | ما اتغيّر |
|---|---|
| `brain/history_learner.py` | `analyze_history()` بيفلتر الذاكرة بالبيلد؛ عينة أقل من 5 لكل اتجاه → 50% محايد بدل نسبة مضللة |
| `brain/trade_dna.py` | `initialize_trade_dna()` boot من آخر 200 صف **مفلترة** (ده كان بيعيد ملء الملف ببيانات قديمة حتى بعد مسحه)؛ `find_similar_trades()` و`rank_strategies()` مفلترين؛ سجلات جديدة بتتوسم بـ `build_id`؛ `get_trade_dna_score()` بيرجع **prior محايد 25.0** بدل 0.0 لما مفيش صفقات حالية مشابهة |
| `core/confidence_engine.py` | `score_dna()` بيتخطى أي صف مش من البيلد الحالي؛ العينة الصغيرة ترجع القيمة المحايدة 4 زي الأول |
| `core/self_optimizer.py` | `_analyze_trades()` وعدّاد دورة التحسين على صفقات البيلد الحالي فقط؛ قلة العينة → الأوزان الافتراضية تفضل زي ما هي |
| `core/adaptive_weighting.py` | `_deep_analyze()` وفترة الـ 100 صفقة بين دورات التكيّف على صفقات البيلد الحالي فقط |

### 3. `tools/dedupe_adaptive_trades.py` (جديد)
`data/analytics/adaptive_trades.jsonl` كان فيه **49 سطر مكرر حرفيًا** (1007 → 958
فريد، على الأغلب sync اتنفذ أكتر من مرة). الأداة تشيل التكرار مع الحفاظ على
الترتيب، idempotent، وبتاخد backup قبل الكتابة (`--apply`). اتنفذت على النسخة
المرفقة.

## السلوك بعد الباتش

- القرار الحي (dna + history + confidence-dna + محركات الأوزان) بداية نضيفة
  فعلية: لا يتأثر بأي صفقة قبل `2026-08-19T15:41:47Z`.
- لحد ما تتجمع عينة كفاية من البيلد الحالي، المكونات بترجع priors محافظة
  (dna=25، history=50/50، confidence-dna=4، أوزان افتراضية) — من غير فترة
  "عمى" بدرجات صفر تسحق الـ master_score.
- الأرشيف كاملًا محفوظ: `ai_memory.csv`، `trade_dna_v2.json`، quarantine —
  متاحين للتدقيق والتحقق من الإصلاح.

## التحقق المنفذ

- `py_compile` على الملفات السبعة: سليم.
- اختبار وظيفي: الفلترة على 1731 صف ذاكرة → 0 صفقات من البيلد الحالي (صحيح:
  آخر صف 14:35 قبل cutoff 15:41) → كل المكونات رجعت priors محافظة كما هو
  مصمم؛ صفقة اختبارية بعد الـ cutoff عدّت الفلتر بنجاح.
- dedupe jsonl: 1007 → 958 سطر.
